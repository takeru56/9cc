{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C debug",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/9cc",
            "args": ["'return 1;'"],
            "cwd": "${fileDirname}",
            "preLaunchTask": "C build",
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb"
        }
    ]
}